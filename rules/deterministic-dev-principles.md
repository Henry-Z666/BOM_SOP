# 确定性开发原则

- 所有运行事实必须来自 BOM、Creo、版本化配置或结构化人工输入。
- 任何自动接受都必须能由仓库内规则、Artifact 和哈希独立复算。
- 禁止自由文本生成 occurrence、坐标、装配关系、相机、PAN、Zoom 或完成状态。
- 缺少几何证据时保持步骤待确认，不用概率推断填补事实。
- 固定双视角只在正式渲染前选择一次，图片结果不能反向改变相机。
- 规则、错误码和恢复边界必须写入仓库，不依赖聊天历史或外部模型状态。
- 产品特定路径、许可证、occurrence 与坐标不得写入通用代码或已提交配置。
