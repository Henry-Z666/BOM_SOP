# 清洁迁移验收记录（2026-08-14）

## 隔离条件

- 新建 `data/clean-migration-20260814-01`，未复制旧合同、旧图片、旧缓存或产品脚本；
- 输入只有 `BOM.xlsx` 与包含 106 个文件的 `CAD/`；
- 在该目录创建独立 64 位 Python 3.12 环境并安装仓库 Agent；
- 安装版本包含 PySide6 6.11.1、DashScope 1.26.7、pywin32 312、openpyxl 3.1.5；
- API Key 只从已有 `DASHSCOPE_API_KEY` 环境变量读取，未输出或写入文件。

## 已通过

| 验收项 | 结果 |
| --- | --- |
| 安装后两项输入分析 | 识别 63 行 BOM、9 个主工序、8 个出图主工序、52 个最高版本模型文件及 `jb9918900337.asm.2` |
| 生成前统一确认 | 本批次无确认问题；正常锁定一个 PlanRevision |
| 黑箱安全降级 | 未证明通用 Creo 几何时输出 11 个明显占位步骤和 `SOP_待确认.xlsx`，没有猜测安装图 |
| 交付白名单 | 用户目录仅有 `SOP_待确认.xlsx` 与 `步骤图片/` |
| 源 CAD 只读 | 106 个源文件与复制输入逐文件 SHA-256 一致 |
| 真实 Creo discovery | Agent 公共接口从两项输入自动锁定 `jb9918900337.asm.2`，在一次性模型副本中导出 87 个 occurrence 和 182 条原生约束 |
| 约束几何证据 | 装配侧 167/182、零件侧 168/182 可取得根坐标曲面法向或轴向；无模型项的 `FIX` 约束明确保持 unavailable |
| BOM/occurrence 映射 | 56 个实体 BOM 行全部映射；4 个重复紧固件行由同工序接收件与原生约束自动消歧，最终 0 歧义、0 缺失、0 数量不足 |
| discovery 进程鲁棒性 | 修复 Creo 派生进程继承捕获管道导致的等待；J-Link 输出改写批次内部日志，Agent 端到端分析 27 秒返回 |
| 普通 Excel | 64 位 pywin32 成功只读打开工作簿、检查图片与打印区域、导出临时 PDF |
| Qwen 文本 | 官方 DashScope SDK 真实调用把普通语言转为受限 `StepRevision`；错误 Schema 可在 3 次内纠正 |
| Qwen-VL | 只发送占位渲染图和最小上下文，正确返回 `passed=false` 及三个可读性问题 |
| 64 位桌面包 | 确定性构建入口先编译 J-Link 再调用 PyInstaller，生成 76,833,368 字节的无控制台 v3d EXE；正式包只携带通用 discovery 运行文件，不再打包旧产品专用 Creo 脚本 |
| 冻结后台 IPC | v3d EXE 使用工作区内原子请求/响应文件并真实启动 Creo；40.9 秒返回 87/182、56 行全匹配和 0 个问题 |
| 冻结 discovery 完成门 | Java 只在 `connection.End()` 后写原子完成标记；外层 PowerShell 句柄异常时可有界清理，不把“JSON 已写”误判为会话已结束 |

阶段构建物保存在忽略的隔离工作区，不作为仓库源码提交。最新 v3d EXE SHA-256 为
`14fe8ccc05be3e3570d284afd9edbb852e5077c768696477b9b39125c8e5cde5`。

## 尚未通过，不得视为最终迁移完成

1. v3 discovery 和 BOM/occurrence 映射已经接通，但尚未把每个工序的 moving、receiver、
   纯平移方向和双固定相机编译成通用正式 render plan；当前仍安全交付待确认占位。
2. 未在清洁工作区用真实 Creo 生成 42 张正式水箱图，也未得到全通过的 `SOP.xlsx`。
3. 10 次一致性目前覆盖两输入文件分析，不覆盖真实 discovery 图谱、图片像素、箭头端点和完整出版。
4. 500 任务测试使用假 Creo worker；尚未验证真实许可证、会话重启和强杀恢复。
5. 因此阶段七状态仍为“部分通过”。下一开发重点是把已验证的 v3 约束几何证据编译成
   产品无关 render plan，而不是把旧水箱合同复制进安装包。
