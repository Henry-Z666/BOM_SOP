[project]
name = "creo-native-sop"
version = "0.1.0"
description = "Auditable, review-gated Creo-native assembly SOP pipeline"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26", "Pillow>=10"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
